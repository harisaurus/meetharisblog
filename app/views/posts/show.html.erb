<div class="branding">
  <%= render 'layouts/svg' %>
</div>

<div class="right-actions">
  <% if user_signed_in? %>
    <%= link_to edit_post_path(@post), class: "action-item" do %>
      <span class="icon-compose"></span>
    <% end %>
  <% end %>

  <%= link_to posts_path, class: "action-item" do %>
    <span class="icon-close"></span>
  <% end %>
</div>

<div class="show-post scene_element scene_element--fadeinup">
  <p id="notice"><%= notice %></p>
  
  <div class="post-block">
    <h1 class="u-spacer10 "><%= link_to_unless_current @post.title, @post %></h1>
    <h3 class="u-spacer50"> Created <%= time_ago_in_words(@post.created_at) %> ago</h3 class="u-spacer10">
    
    <% if @post.tag_list.present? %>
      <p>
        <span class="icon-tag"></span>
        <% @post.tag_list.each do |tagitem| %>
          <span class="tag"><%= tagitem %></span>
        <% end %>
      </p>
    <% end %>

    <div class="markdown">
      <%= markdown @post.body %>
    </div>
  </div>

  <!-- <div class="post-block">
    <div class="comments-holder">
    	<h2><%= @post.comments.count %> Comments</h2>
    	<%= render @post.comments %>

    	<h3>Add a comment:</h3>
    	<%= render "comments/form" %>
    </div>
  </div> -->

  <%= render 'layouts/footer' %>
  
</div>